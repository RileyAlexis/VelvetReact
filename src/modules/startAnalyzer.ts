import { AudioData } from "../interfaces"

export const startAnalyzer = async (audioContext: AudioContext,
    source: MediaStreamAudioSourceNode | AudioBufferSourceNode) => {

    console.log(source);



}